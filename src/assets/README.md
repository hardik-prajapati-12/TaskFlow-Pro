Static assets (images, illustrations, custom fonts) go here and are imported
directly in components, e.g. `import logo from '@/assets/logo.svg'`.

TaskFlow Pro currently ships icon-free of raster assets — all iconography
comes from `react-icons`, and the brand mark is inline SVG in `public/favicon.svg` —
so this folder is intentionally empty, ready for whatever you add next.
